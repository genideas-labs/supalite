# Tasks: `supalite db pull` — Baseline Schema Dump (001)

**Input**: [spec.md](spec.md), [plan.md](plan.md), [contracts/cli-contract.md](contracts/cli-contract.md), [quickstart.md](quickstart.md), [research.md](research.md)
**Approach**: live-Postgres TDD (repo convention — tests hit `DB_CONNECTION`); commit after every task.
**Revised** after strict pre-implementation review round 1 (17 findings fixed — see tracking issue #5).

## User Stories

- **US1 (P1)**: As a developer, I get a dependency-ordered, idempotent
  baseline SQL from a live database that round-trips onto an empty DB and
  re-applies to the source DB with zero errors (spec scenarios 1–3, 8;
  SC-001/002/004; FR-003–FR-016, FR-021).
- **US2 (P2)**: As a developer, I control safety filters — extension-owned
  objects excluded by default with an opt-in include flag, and a plain-DDL
  mode (spec scenarios 4, 7; SC-003; FR-007/008/018/019).
- **US3 (P3)**: As a developer, I invoke everything through the
  `supalite db pull` CLI with `gen types`-consistent flags, default out path,
  stdout mode, and a clear diff-mode error (spec scenarios 1, 5, 6; SC-005;
  FR-001/002/017).

## Phase 1: Setup

- [X] T001 Create fixture seed `scripts/seed-db-pull.sql` for schema `db_pull_schema` containing: enum `order_status` ('pending','paid','cancelled') + enum `weird_labels` (one label with an embedded single quote, e.g. `it''s`); domain `positive_int AS integer CHECK (VALUE > 0)`; composite `money_pair (amount numeric, currency text)` + composite-on-composite `money_bag (pairs db_pull_schema.money_pair[], note text)` (array-of-composite edge); standalone sequence `invoice_seq` START 1000 INCREMENT 5 CACHE 10; serial-style sequence `legacy_id_seq`; IMMUTABLE function `norm_email(text)` (type-stage); plpgsql function `crlf_fn()` whose body is seeded with CRLF via `E'...\r\n...'`; table `customers` (identity ALWAYS pk, `email text NOT NULL` UNIQUE `customers_email_key`, CHECK `customers_email_check`, CHECK `customers_dollar_check CHECK (email <> 'a$$b')` (literal `$$` in guard content), CHECK `customers_tag_check CHECK (email <> 'x$supalite$y')` (collides with the first tag candidate → fallback `$supalite1$`), column `norm text DEFAULT db_pull_schema.norm_email('X@Y')` → deferred-default path, column `code text COLLATE "C"`, generated column calling `norm_email` (type-stage fn → must replay), expression index `customers_email_norm_idx` ON `(db_pull_schema.norm_email(email))`, standalone unique index `customers_full_name_uidx`, `ENABLE ROW LEVEL SECURITY` + `CREATE POLICY customers_self`); table `orders` (identity BY DEFAULT (START 500 INCREMENT 2) pk, `legacy_id integer NOT NULL DEFAULT nextval('db_pull_schema.legacy_id_seq')`, `qty db_pull_schema.positive_int` (domain-typed column), FK `orders_customer_id_fkey` → customers, `total numeric(12,2) DEFAULT 0`, generated `total_with_tax ... STORED`, CHECK `orders_total_check`, CHECK `orders_norm_check` calling `norm_email`, EXCLUDE `orders_legacy_id_excl EXCLUDE (legacy_id WITH =)`, plain index `orders_status_idx`, column `ext_id bigint` + FK `orders_ext_id_fkey` → `db_pull_ext.ext_ref(id)`, deferrable constraint trigger `orders_ct` + plpgsql trigger function, plain trigger `orders_touch_status` BEFORE UPDATE); `ALTER SEQUENCE legacy_id_seq OWNED BY orders.legacy_id`; table `snapshots (snap db_pull_schema.customers)` (table-row-type column → table topo); hostile-identifier table `"CamelTable"` (columns `"order" text` and `"weird""col" text` (embedded double quote), CHECK named `"Check Me"`); UNLOGGED table `session_cache`; plpgsql procedure `log_noop()`; SQL function `order_count(bigint)`; stage-B function `user_rows() RETURNS SETOF db_pull_schema.customers`; view `paid_orders`; view-on-view `paid_order_totals`; stage-C function `paid_orders_all() RETURNS SETOF db_pull_schema.paid_orders`; `security_barrier` view `safe_orders`; matview `order_totals_mv` + unique index `order_totals_mv_uidx`; unpopulated matview `empty_mv` (`WITH NO DATA`); footer exercisers: partitioned table `events_partitioned` PARTITION BY RANGE + leaf partition `events_p1`, table `events_ref` with FK → `events_partitioned` (FK-to-excluded-relation), aggregate `agg_sum(bigint) (SFUNC=int8pl, STYPE=bigint)` + view `agg_totals` selecting `agg_sum(...)` from orders (aggregate-dependent view), view `events_view AS SELECT * FROM events_partitioned` (partition-parent-dependent view), function `get_paid(OUT o db_pull_schema.paid_orders)` (OUT-param view classification); helper schema `db_pull_ext` with table `ext_ref (id bigint PRIMARY KEY)` (stays OUTSIDE the pulled selection — documented pre-existing prerequisite). Cleanup `scripts/cleanup-db-pull.sql`: `DROP SCHEMA IF EXISTS db_pull_schema CASCADE; DROP SCHEMA IF EXISTS db_pull_ext CASCADE;`

## Phase 2: Foundational

- [X] T002 Create test harness `src/__tests__/db-pull.test.ts` following `src/__tests__/gen-types.test.ts` (dotenv, `Pool`, seed in `beforeAll` / cleanup in `afterAll`, shared `baseline` generated once) with first failing assertions: header banner `-- supalite db pull baseline`, `SET check_function_bodies = off;`, `CREATE SCHEMA IF NOT EXISTS db_pull_schema;`, no `\r`, output ends with exactly one trailing newline
- [X] T003 Create `src/db-pull.ts` skeleton making T002 pass: `DbPullOptions`, internal `Ctx`, helpers (`normalizeLf`, `escapeLiteral`, `extensionFilter(ctx, classCatalog, oidExpr)`), `renderSchemas` (selected non-public + extension target namespaces, quote_ident, IF NOT EXISTS always), `renderExtensions` (skip plpgsql, Kahn topo over extension→extension `pg_depend` edges with oid tie-break, `CREATE EXTENSION IF NOT EXISTS ... WITH SCHEMA ...` always), `generateBaselineSql` (defaults per contract; Client connect/end in finally; `-- <section>` banners; empty-selection suppression: when all object sections are empty, drop schemas/extensions sections and `console.warn`; LF normalize + single trailing newline)
- [X] T004 Export from package root: add `export { generateBaselineSql } from './db-pull'; export type { DbPullOptions } from './db-pull';` to `src/index.ts` + test importing `generateBaselineSql` from `'../index'` in `src/__tests__/db-pull.test.ts` (FR-020)

## Phase 3: US1 — Core baseline generator (MVP)

- [X] T005 [US1] Sequences: failing assertions (`CREATE SEQUENCE IF NOT EXISTS db_pull_schema.invoice_seq` with `START WITH 1000`/`INCREMENT BY 5`/`CACHE 10`; contains `legacy_id_seq`; does NOT contain identity-internal sequences e.g. `customers_id_seq`) then implement `renderSequences` in `src/db-pull.ts` (`pg_sequence` join `pg_class`, exclude `pg_depend deptype='i'`, extension filter, options always explicit)
- [X] T006 [US1] Types: failing assertions (`AS ENUM ('pending', 'paid', 'cancelled')`, escaped quote label `it''s`, `CREATE DOMAIN db_pull_schema.positive_int AS integer` with its CHECK, composite `AS (amount numeric, currency text)`, `money_pair` emitted BEFORE `money_bag` (array-of-composite edge resolved via `typelem`), duplicate-object guards use a content-safe dollar tag (customers' `$$`-containing CHECK is in §constraints, but the tag helper is shared — assert `$supalite` tag scheme when content contains `$$`)) then implement `renderTypes` in `src/db-pull.ts` (enums via `pg_enum`; domains via `pg_type typtype='d'` + base `format_type` + default + NOT NULL + `pg_get_constraintdef`; composites via `relkind='c'` + `pg_attribute`; ONE unified Kahn topo-sort with edges through attribute/base types resolving arrays via `pg_type.typelem`; composites/domains whose attributes/base reference relation row types → exclusion-closure divert to footer; domain defaults/CHECKs calling user functions split out to deferred `ALTER DOMAIN ... SET DEFAULT` (§deferred defaults) / guarded `ALTER DOMAIN ... ADD CONSTRAINT` (§constraints, guard checks `conname` + `contypid='<domain>'::regtype` since domain constraints have `conrelid=0`); label escaping; `wrapDuplicateGuard(content)` picking first of `$supalite$`, `$supalite1$`, ... absent from content, only when `ifNotExists`)
- [X] T007 [US1] Tables + sequence ownership: failing assertions (`CREATE TABLE IF NOT EXISTS db_pull_schema.customers (`, `id bigint GENERATED ALWAYS AS IDENTITY`, `id bigint GENERATED BY DEFAULT AS IDENTITY (START WITH 500 INCREMENT BY 2)`, `DEFAULT nextval('db_pull_schema.legacy_id_seq'::regclass)`, generated columns `GENERATED ALWAYS AS` + `STORED` (incl. the one calling `norm_email` — type-stage fn already emitted), `qty db_pull_schema.positive_int` domain column, `code text COLLATE "C"`, `email text NOT NULL`, `CREATE UNLOGGED TABLE IF NOT EXISTS db_pull_schema.session_cache`, `"CamelTable"` / `"order"` / `"weird""col"` correctly quoted, CHECKs not inlined, plain-default `norm` column has NO inline DEFAULT (deferred), `customers` CREATE position < `snapshots` CREATE position (row-type topo), partition leaf `events_p1` NOT created as a table, `ALTER SEQUENCE db_pull_schema.legacy_id_seq OWNED BY db_pull_schema.orders.legacy_id;`) then implement `renderTables` (per-table `pg_attribute`/`pg_attrdef` with identity-seq lateral join via `pg_depend deptype='i'`; `relkind='r' AND NOT relispartition`; Kahn topo over column-row-type edges (arrays resolved via `typelem`); `relpersistence='u'` → UNLOGGED; non-default `attcollation` → `COLLATE`; strip defaults whose `adbin` depends on a user function via `pg_depend`→`pg_proc` and collect for the deferred section; identity options when non-default; NOT NULL skipped on identity columns) and `renderSequenceOwnership` (`pg_depend deptype='a'`, parent-relation extension filter) in `src/db-pull.ts`
- [X] T008 [US1] Staged functions + deferred defaults: failing assertions (type-stage `-- functions` banner BEFORE `-- tables` and contains `norm_email`/`crlf_fn`/`order_count`/`log_noop` (`CREATE OR REPLACE FUNCTION`/`PROCEDURE`, ending `;`); `-- table-dependent functions` AFTER `-- sequence ownership` and contains `user_rows` (`RETURNS SETOF` customers); `-- view-dependent functions` placeholder asserted in T010 after views with `paid_orders_all`; `-- deferred column defaults` AFTER `-- table-dependent functions` containing `ALTER TABLE db_pull_schema.customers ALTER COLUMN norm SET DEFAULT`) plus OUT-param classification: fixture `get_paid(OUT o db_pull_schema.paid_orders)` appears in `-- view-dependent functions` (NOT type-stage), then implement `classifyFunctionStage` (signature scan of `prorettype` + `COALESCE(proallargtypes, proargtypes::oid[])`, arrays via `typelem`, composites via attribute closure → 'type'|'table'|'view'), `renderFunctions(stage)` (`pg_proc prokind IN ('f','p')` + `pg_get_functiondef` + `;`, extension filter) and `renderDeferredDefaults` (table columns AND domain defaults) in `src/db-pull.ts`
- [X] T009 [US1] Constraints + FKs: failing assertions (`ADD CONSTRAINT customers_pkey PRIMARY KEY (id)`, CHECK `orders_norm_check` (function-calling) emitted in `-- constraints` (after functions), EXCLUDE `orders_legacy_id_excl` emitted, quoted `"Check Me"` constraint round-trips, FK `orders_customer_id_fkey` in `-- foreign keys` guarded by `DO $$` with `WHERE conname = 'orders_customer_id_fkey'` AND `conrelid = 'db_pull_schema.orders'::regclass`, external FK `orders_ext_id_fkey` emitted as-is, FK from `events_ref` to partitioned `events_partitioned` NOT emitted (collected for footer), guards use `DO $supalite$` (never bare `DO $$`), and `customers_tag_check` (content contains literal `$supalite$`) falls back to `$supalite1$`, last `CREATE TABLE` position < first `FOREIGN KEY` position) then implement `renderConstraints` in `src/db-pull.ts` (`pg_constraint contype IN ('p','u','c','f','x')`, `conparentid = 0`, parent relation `relkind='r' AND NOT relispartition` + extension filter, `pg_get_constraintdef`, `format('%I.%I')` regclass literal, two passes non-FK then FK, FKs to excluded relations diverted to footer, guard only when `ifNotExists`; collect external-schema FK refs for footer)
- [X] T010 [US1] Views: failing assertions (`CREATE OR REPLACE VIEW db_pull_schema.paid_orders` position < `paid_order_totals` position; `safe_orders` rendered with `security_barrier`; `CREATE MATERIALIZED VIEW IF NOT EXISTS db_pull_schema.order_totals_mv`; `empty_mv` rendered `WITH NO DATA`; aggregate-dependent `agg_totals` NOT emitted (collected for footer); `-- view-dependent functions` banner AFTER `-- views` containing `paid_orders_all`) then implement `renderViews` in `src/db-pull.ts` (relkind v/m, `pg_get_viewdef(oid, true)` trailing-semicolon strip, Kahn topo-sort over `pg_rewrite`/`pg_depend` view→view edges, view `reloptions` → `WITH (...)`, matview `WITH NO DATA` when `NOT relispopulated`, views whose dependency closure hits an excluded aggregate diverted to footer)
- [X] T011 [US1] Triggers: failing assertions (`CREATE OR REPLACE TRIGGER orders_touch_status BEFORE UPDATE ON db_pull_schema.orders`; constraint trigger `orders_ct` emitted as original `CREATE CONSTRAINT TRIGGER` inside a `DO $$` existence guard keyed on `pg_trigger` tgname+tgrelid, NOT rewritten to OR REPLACE; `-- triggers` banner positioned AFTER `-- views`) then implement `renderTriggers` in `src/db-pull.ts` (`NOT tgisinternal`, parent-relation extension filter, `pg_get_triggerdef`; plain → `CREATE OR REPLACE TRIGGER` only when `ifNotExists`; `tgconstraint <> 0` → guard)
- [X] T012 [US1] Indexes: failing assertions (`CREATE INDEX IF NOT EXISTS orders_status_idx ON db_pull_schema.orders USING btree (status)`, expression index containing `norm_email`, `CREATE UNIQUE INDEX IF NOT EXISTS customers_full_name_uidx`, matview index `order_totals_mv_uidx` present, `-- indexes` banner AFTER `-- views`, constraint-backing indexes absent: no index DDL for `customers_email_key` / `customers_pkey` / `orders_legacy_id_excl`) then implement `renderIndexes` in `src/db-pull.ts` (`pg_index` + `pg_get_indexdef`, relations `relkind IN ('r','m') AND NOT relispartition`, exclude via `pg_constraint.conindid`, anchored `IF NOT EXISTS` insertion for both `CREATE INDEX ` and `CREATE UNIQUE INDEX ` when `ifNotExists`)
- [X] T013 [US1] Footer + section order: failing assertions (footer lists `partitioned table: db_pull_schema.events_partitioned` AND its leaf `db_pull_schema.events_p1`, `aggregate/window function: db_pull_schema.agg_sum`, omitted dependents: views `agg_totals` + `events_view` and FK `events_ref`→`events_partitioned` (exclusion closure), external FK reference to `db_pull_ext`; full banner order strictly: schemas < sequences < types < functions < tables < sequence ownership < table-dependent functions < deferred column defaults < constraints < foreign keys < views < view-dependent functions < triggers < indexes) then implement `renderFooter` in `src/db-pull.ts` (partition hierarchy via `relkind='p'` + `relispartition`, aggregates `prokind IN ('a','w')`, transitive exclusion closure over `pg_depend` fetched once and propagated to fixpoint (partition parents/leaves + aggregates + extension-owned roots → dependent views/FKs/constraints/deferred defaults/relation-referencing composites/functions-with-excluded-signatures), non-reproduced variant detection: typed tables `reloftype<>0`, inheritance via `pg_inherits` non-partition, non-default `relam`/`reltablespace`/`reloptions`/`relreplident`, generated columns calling table/view-stage functions; plus external FK refs from T009)
- [X] T014 [US1] Round-trip + idempotency tests in `src/__tests__/db-pull.test.ts` (SC-001: generate → `DROP SCHEMA db_pull_schema CASCADE` → apply baseline → regenerate → equality with comment lines stripped; SC-002: apply the same baseline again on the built schema, expect no error — covers constraint guards, constraint trigger guard, matview IF NOT EXISTS) — must run last in the file; fix any renderer instability they expose

## Phase 4: US2 — Safety filters & modes

- [X] T015 [US2] Plain-mode + edge tests in `src/__tests__/db-pull.test.ts`: (a) `ifNotExists: false` → no line starts with `DO $` (generic — catches tagged guards too), plain `CREATE TYPE`/`CREATE DOMAIN`, plain `CREATE TRIGGER `, plain constraint trigger, no `IF NOT EXISTS` outside `CREATE SCHEMA`/`CREATE EXTENSION` lines, still `CREATE OR REPLACE FUNCTION` (FR-007); (b) empty selection (`schemas: ['db_pull_nonexistent']`) → truly header-only output (no banners, no CREATE SCHEMA/EXTENSION) + stderr warning via `console.warn` spy (FR-018); (c) baseline contains neither `GRANT ` nor `CREATE POLICY` nor `ROW LEVEL SECURITY` despite the seeded policy (FR-019) — fix any leaking renderer in `src/db-pull.ts`
- [X] T016 [US2] Extension-filter tests in `src/__tests__/db-pull.test.ts`: conditional pg_trgm case (try `CREATE EXTENSION IF NOT EXISTS pg_trgm`, skip gracefully if unavailable, restore pre-existence state after): default output contains `CREATE EXTENSION IF NOT EXISTS pg_trgm` but NO DDL for any object whose name contains `gtrgm` in ANY section (functions/types/operators leak check); `includeExtensionObjects: true` → `gtrgm` function DDL present; verify parent-relation filtering (extension filter applied in constraints/ownership/triggers queries per plan) — fix leaks in `src/db-pull.ts` (SC-003, FR-008)

## Phase 5: US3 — CLI subcommand

- [X] T017 [US3] Restructure `src/cli.ts`: move existing body into `runGenTypes(rawArgs)` unchanged, add dispatcher `run()` (`gen types` | `db pull` | print both usages + exit 1), add `parseDbPullArgs` (`--db-url`, `--schema/--schemas` comma-split accumulating, `--out`, `--mode`, `--include-extension-objects`, `--no-if-not-exists`, `--help`), `printDbPullUsage` (documents opt-out defaults + PG14+ trigger note), `utcTimestamp()` (`YYYYMMDDHHMMSS` from ISO string), `runDbPull` (DB_CONNECTION fallback; exact diff error `Only --mode baseline is supported in this version (diff is planned).` exit 1; stdout for `-`/`stdout`; default path `supabase/migrations/<ts>_baseline.sql` with `fs.mkdir recursive`; success log `Wrote baseline schema to <path>`)
- [X] T018 [US3] Automated CLI tests `src/__tests__/db-pull-cli.test.ts` spawning `npx ts-node src/cli.ts db pull ...` via `child_process.execFile` with isolated env + temp cwd: `--help` AND `-h` exit 0 listing flags; missing db-url (env without DB_CONNECTION) → exit 1 + usage; `DB_CONNECTION` fallback works (env-injected, `--out -` emits header); `--out stdout` alias behaves like `-`; explicit `--out <tmp>/x/base.sql` creates nested dir + file; connection failure (unreachable URL) → exit 1 + stderr message; `--mode diff` → exit 1 + exact message; default path run in temp dir creates `supabase/migrations/` + filename matches `/^\d{14}_baseline\.sql$/`; comma `--schema db_pull_schema,public` AND repeated `--schema a --schema b` accumulation (FR-001/002/017, SC-005)
- [X] T019 [US3] Manual quickstart smoke per `specs/001-db-pull/quickstart.md` §2–§6 after `npm run build` (stdout, default path, mode guard, re-apply via psql if available, help) — record actual outputs in the commit message

## Phase 6: Polish & cross-cutting

- [X] T020 [P] Update `README.md`: replace roadmap line "`supalite db pull` wrapper around `pg-schema-sync` for schema sync" with remaining future work (`--mode diff`), rewrite the "About `supabase db pull`" paragraph to point at the native command, add usage subsection under "Migrations and schema management" (flags, opt-out defaults, idempotency guarantee incl. constraint guards, extension filter, section order, v1 limitations incl. footer-listed variants, PG14+ trigger replay note)
- [X] T021 [P] Mirror the same changes in `README.ko.md` (roadmap line, "`supabase db pull`에 대해서" paragraph, 마이그레이션 섹션 사용법)
- [X] T022 [P] Add `CHANGELOG.md` entry under `## [Unreleased]` → `### Added` describing `supalite db pull` (issue #4): dependency-ordered baseline, idempotent defaults (constraint DO guards), extension-object exclusion, coverage list, v1 limitations
- [X] T023 Full regression gate: `npm test && npm run lint && npm run build` all green; confirm `gen-types.test.ts` untouched and passing

## Dependencies

```
T001 → T002 → T003 → T004 → T005 → T006 → T007 → T008 → T009 → T010 → T011 → T012 → T013 → T014
                                                                                          ↓
                                                                              T015 → T016 (US2)
                                                                                          ↓
                                                                              T017 → T018 → T019 (US3)
                                                                                          ↓
                                                                  T020 ∥ T021 ∥ T022 → T023
```

- US1 (T005–T014) is strictly sequential: single module + single test file;
  each renderer slots into the fixed section order.
- US2 depends on all renderers (T014 done); US3 depends on final option
  semantics (T016 done); T020–T022 are parallelizable [P]; T023 is the gate.

## Implementation strategy

- **MVP = US1** (T001–T014): after T014 the library API delivers the full
  guarantee (round-trip + idempotency) without a CLI.
- Commit after every task (`feat(db-pull): ...` / `test(db-pull): ...` /
  `docs: ...`), keeping `npm test -- db-pull` green at each step.
- Every test command runs against live Postgres via `DB_CONNECTION`
  (default `postgresql://testuser:testpassword@localhost:5432/testdb`).
