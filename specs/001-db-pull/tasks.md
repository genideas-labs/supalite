# Tasks: `supalite db pull` — Baseline Schema Dump (001)

**Input**: [spec.md](spec.md), [plan.md](plan.md), [contracts/cli-contract.md](contracts/cli-contract.md), [quickstart.md](quickstart.md), [research.md](research.md)
**Approach**: live-Postgres TDD (repo convention — tests hit `DB_CONNECTION`); commit after every task.
**Revised** after strict pre-implementation review round 1 (17 findings fixed — see tracking issue #5).

## User Stories

- **US1 (P1)**: As a developer, I get a dependency-ordered, idempotent
  baseline SQL from a live database that round-trips onto an empty DB and
  re-applies to the source DB with zero errors (spec scenarios 1–3, 8;
  SC-001/002/004; FR-003–FR-016, FR-021).
- **US2 (P2)**: As a developer, I control safety filters — extension-owned
  objects excluded by default with an opt-in include flag, and a plain-DDL
  mode (spec scenarios 4, 7; SC-003; FR-007/008/018/019).
- **US3 (P3)**: As a developer, I invoke everything through the
  `supalite db pull` CLI with `gen types`-consistent flags, default out path,
  stdout mode, and a clear diff-mode error (spec scenarios 1, 5, 6; SC-005;
  FR-001/002/017).

## Phase 1: Setup

- [ ] T001 Create fixture seed `scripts/seed-db-pull.sql` for schema `db_pull_schema` containing: enum `order_status` ('pending','paid','cancelled') + enum `weird_labels` (one label with an embedded single quote, e.g. `it''s`); composite `money_pair (amount numeric, currency text)` + composite-on-composite `money_bag (pair db_pull_schema.money_pair, note text)`; standalone sequence `invoice_seq` START 1000 INCREMENT 5 CACHE 10; serial-style sequence `legacy_id_seq`; IMMUTABLE function `norm_email(text)` (e.g. `lower(trim($1))`); table `customers` (identity ALWAYS pk, `email text NOT NULL` UNIQUE `customers_email_key`, CHECK `customers_email_check` using `position('@' in email) > 1`, column `norm text DEFAULT db_pull_schema.norm_email('X@Y')` → deferred-default path, expression index `customers_email_norm_idx` ON `(db_pull_schema.norm_email(email))`, standalone unique index `customers_full_name_uidx`, `ENABLE ROW LEVEL SECURITY` + `CREATE POLICY customers_self`); table `orders` (identity BY DEFAULT (START 500 INCREMENT 2) pk, `legacy_id integer NOT NULL DEFAULT nextval('db_pull_schema.legacy_id_seq')`, FK `orders_customer_id_fkey` → customers, `total numeric(12,2) DEFAULT 0`, generated column `total_with_tax ... STORED`, CHECK `orders_total_check`, CHECK `orders_norm_check` calling `norm_email`, EXCLUDE `orders_legacy_id_excl EXCLUDE (legacy_id WITH =)`, plain index `orders_status_idx`, column `ext_id bigint` + FK `orders_ext_id_fkey` → `db_pull_ext.ext_ref(id)`, deferrable constraint trigger `orders_ct` + plpgsql trigger function, plain trigger `orders_touch_status` BEFORE UPDATE); `ALTER SEQUENCE legacy_id_seq OWNED BY orders.legacy_id`; hostile-identifier table `"CamelTable"` (column `"order" text`, CHECK named `"Check Me"`); UNLOGGED table `session_cache`; plpgsql procedure `log_noop()`; SQL function `order_count(bigint)`; view `paid_orders`; view-on-view `paid_order_totals`; `security_barrier` view `safe_orders` (`WITH (security_barrier)`); matview `order_totals_mv` + unique index `order_totals_mv_uidx`; unpopulated matview `empty_mv` (`WITH NO DATA`); footer exercisers: partitioned table `events_partitioned` PARTITION BY RANGE + leaf partition `events_p1`, aggregate `agg_sum(bigint) (SFUNC=int8pl, STYPE=bigint)`, domain `positive_int`; helper schema `db_pull_ext` with table `ext_ref (id bigint PRIMARY KEY)` (stays OUTSIDE the pulled selection). Cleanup `scripts/cleanup-db-pull.sql`: `DROP SCHEMA IF EXISTS db_pull_schema CASCADE; DROP SCHEMA IF EXISTS db_pull_ext CASCADE;`

## Phase 2: Foundational

- [ ] T002 Create test harness `src/__tests__/db-pull.test.ts` following `src/__tests__/gen-types.test.ts` (dotenv, `Pool`, seed in `beforeAll` / cleanup in `afterAll`, shared `baseline` generated once) with first failing assertions: header banner `-- supalite db pull baseline`, `SET check_function_bodies = off;`, `CREATE SCHEMA IF NOT EXISTS db_pull_schema;`, no `\r`, output ends with exactly one trailing newline
- [ ] T003 Create `src/db-pull.ts` skeleton making T002 pass: `DbPullOptions`, internal `Ctx`, helpers (`normalizeLf`, `escapeLiteral`, `extensionFilter(ctx, classCatalog, oidExpr)`), `renderSchemas` (selected non-public + extension target namespaces, quote_ident, IF NOT EXISTS always), `renderExtensions` (skip plpgsql, `ORDER BY e.oid`, `CREATE EXTENSION IF NOT EXISTS ... WITH SCHEMA ...` always), `generateBaselineSql` (defaults per contract; Client connect/end in finally; `-- <section>` banners; empty-selection suppression: when all object sections are empty, drop schemas/extensions sections and `console.warn`; LF normalize + single trailing newline)
- [ ] T004 Export from package root: add `export { generateBaselineSql } from './db-pull'; export type { DbPullOptions } from './db-pull';` to `src/index.ts` + test importing `generateBaselineSql` from `'../index'` in `src/__tests__/db-pull.test.ts` (FR-020)

## Phase 3: US1 — Core baseline generator (MVP)

- [ ] T005 [US1] Sequences: failing assertions (`CREATE SEQUENCE IF NOT EXISTS db_pull_schema.invoice_seq` with `START WITH 1000`/`INCREMENT BY 5`/`CACHE 10`; contains `legacy_id_seq`; does NOT contain identity-internal sequences e.g. `customers_id_seq`) then implement `renderSequences` in `src/db-pull.ts` (`pg_sequence` join `pg_class`, exclude `pg_depend deptype='i'`, extension filter, options always explicit)
- [ ] T006 [US1] Types: failing assertions (`AS ENUM ('pending', 'paid', 'cancelled')`, escaped quote label `it''s`, composite `AS (amount numeric, currency text)`, `money_pair` composite emitted BEFORE `money_bag` (composite-on-composite topo order), wrapped in `DO $$`/`EXCEPTION WHEN duplicate_object`) then implement `renderTypes` in `src/db-pull.ts` (enums via `pg_enum`; composites via `pg_class relkind='c'` + `pg_attribute` `format_type` with Kahn topo-sort over attribute-type edges; label escaping; `wrapDuplicateGuard` only when `ifNotExists`)
- [ ] T007 [US1] Tables + sequence ownership: failing assertions (`CREATE TABLE IF NOT EXISTS db_pull_schema.customers (`, `id bigint GENERATED ALWAYS AS IDENTITY`, `id bigint GENERATED BY DEFAULT AS IDENTITY (START WITH 500 INCREMENT BY 2)`, `DEFAULT nextval('db_pull_schema.legacy_id_seq'::regclass)`, `GENERATED ALWAYS AS` + `STORED`, `email text NOT NULL`, `CREATE UNLOGGED TABLE IF NOT EXISTS db_pull_schema.session_cache`, `"CamelTable"` and `"order"` correctly quoted, CHECKs not inlined in CREATE TABLE, `norm_email` NOT present inside any `CREATE TABLE` (function-dependent default deferred), partition leaf `events_p1` NOT created as a table, `ALTER SEQUENCE db_pull_schema.legacy_id_seq OWNED BY db_pull_schema.orders.legacy_id;`) then implement `renderTables` (per-table `pg_attribute`/`pg_attrdef` with identity-seq lateral join via `pg_depend deptype='i'`; `relkind='r' AND NOT relispartition`; `relpersistence='u'` → UNLOGGED; strip defaults whose `adbin` depends on a user function via `pg_depend`→`pg_proc` and collect them for §9; identity options when non-default; NOT NULL skipped on identity columns) and `renderSequenceOwnership` (`pg_depend deptype='a'`, parent-relation extension filter) in `src/db-pull.ts`
- [ ] T008 [US1] Functions + deferred defaults: failing assertions (`CREATE OR REPLACE FUNCTION db_pull_schema.order_count`, `CREATE OR REPLACE FUNCTION db_pull_schema.norm_email`, `CREATE OR REPLACE PROCEDURE db_pull_schema.log_noop`, statements end with `;`, `-- deferred column defaults` section contains `ALTER TABLE db_pull_schema.customers ALTER COLUMN norm SET DEFAULT` and appears AFTER `-- functions`) then implement `renderFunctions` (`pg_proc prokind IN ('f','p')` + `pg_get_functiondef` + `;`, extension filter) and `renderDeferredDefaults` (from T007's collected list) in `src/db-pull.ts`
- [ ] T009 [US1] Constraints + FKs: failing assertions (`ADD CONSTRAINT customers_pkey PRIMARY KEY (id)`, CHECK `orders_norm_check` (function-calling) emitted in `-- constraints` (after functions), EXCLUDE `orders_legacy_id_excl` emitted, quoted `"Check Me"` constraint round-trips, FK `orders_customer_id_fkey` in `-- foreign keys` guarded by `DO $$` with `WHERE conname = 'orders_customer_id_fkey'` AND `conrelid = 'db_pull_schema.orders'::regclass`, external FK `orders_ext_id_fkey` emitted as-is, last `CREATE TABLE` position < first `FOREIGN KEY` position) then implement `renderConstraints` in `src/db-pull.ts` (`pg_constraint contype IN ('p','u','c','f','x')`, `conparentid = 0`, parent relation `relkind='r' AND NOT relispartition` + extension filter, `pg_get_constraintdef`, `format('%I.%I')` regclass literal, two passes non-FK then FK, guard only when `ifNotExists`; collect external-schema FK refs for footer)
- [ ] T010 [US1] Views: failing assertions (`CREATE OR REPLACE VIEW db_pull_schema.paid_orders` position < `paid_order_totals` position; `safe_orders` rendered with `security_barrier`; `CREATE MATERIALIZED VIEW IF NOT EXISTS db_pull_schema.order_totals_mv`; `empty_mv` rendered `WITH NO DATA`) then implement `renderViews` in `src/db-pull.ts` (relkind v/m, `pg_get_viewdef(oid, true)` trailing-semicolon strip, Kahn topo-sort over `pg_rewrite`/`pg_depend` view→view edges, view `reloptions` → `WITH (...)`, matview `WITH NO DATA` when `NOT relispopulated`)
- [ ] T011 [US1] Triggers: failing assertions (`CREATE OR REPLACE TRIGGER orders_touch_status BEFORE UPDATE ON db_pull_schema.orders`; constraint trigger `orders_ct` emitted as original `CREATE CONSTRAINT TRIGGER` inside a `DO $$` existence guard keyed on `pg_trigger` tgname+tgrelid, NOT rewritten to OR REPLACE; `-- triggers` banner positioned AFTER `-- views`) then implement `renderTriggers` in `src/db-pull.ts` (`NOT tgisinternal`, parent-relation extension filter, `pg_get_triggerdef`; plain → `CREATE OR REPLACE TRIGGER` only when `ifNotExists`; `tgconstraint <> 0` → guard)
- [ ] T012 [US1] Indexes: failing assertions (`CREATE INDEX IF NOT EXISTS orders_status_idx ON db_pull_schema.orders USING btree (status)`, expression index containing `norm_email`, `CREATE UNIQUE INDEX IF NOT EXISTS customers_full_name_uidx`, matview index `order_totals_mv_uidx` present, `-- indexes` banner AFTER `-- views`, constraint-backing indexes absent: no index DDL for `customers_email_key` / `customers_pkey` / `orders_legacy_id_excl`) then implement `renderIndexes` in `src/db-pull.ts` (`pg_index` + `pg_get_indexdef`, relations `relkind IN ('r','m') AND NOT relispartition`, exclude via `pg_constraint.conindid`, anchored `IF NOT EXISTS` insertion for both `CREATE INDEX ` and `CREATE UNIQUE INDEX ` when `ifNotExists`)
- [ ] T013 [US1] Footer + section order: failing assertions (footer lists `partitioned table: db_pull_schema.events_partitioned` AND its leaf `db_pull_schema.events_p1`, `aggregate/window function: db_pull_schema.agg_sum`, `domain type: db_pull_schema.positive_int`, external FK reference to `db_pull_ext`; full banner order strictly: schemas < sequences < types < tables < sequence ownership < functions < deferred column defaults < constraints < foreign keys < views < triggers < indexes) then implement `renderFooter` in `src/db-pull.ts` (partition hierarchy via `relkind='p'` + `relispartition`, aggregates `prokind IN ('a','w')`, domains `typtype='d'`, non-reproduced variant detection: typed tables `reloftype<>0`, inheritance via `pg_inherits` non-partition, non-default `relam`/`reltablespace`/`reloptions`/`relreplident`, generated columns calling user functions; plus external FK refs from T009)
- [ ] T014 [US1] Round-trip + idempotency tests in `src/__tests__/db-pull.test.ts` (SC-001: generate → `DROP SCHEMA db_pull_schema CASCADE` → apply baseline → regenerate → equality with comment lines stripped; SC-002: apply the same baseline again on the built schema, expect no error — covers constraint guards, constraint trigger guard, matview IF NOT EXISTS) — must run last in the file; fix any renderer instability they expose

## Phase 4: US2 — Safety filters & modes

- [ ] T015 [US2] Plain-mode + edge tests in `src/__tests__/db-pull.test.ts`: (a) `ifNotExists: false` → no `DO $$` except types stay unwrapped too (plain `CREATE TYPE`), plain `CREATE TRIGGER `, plain constraint trigger, no `IF NOT EXISTS` outside `CREATE SCHEMA`/`CREATE EXTENSION` lines, still `CREATE OR REPLACE FUNCTION` (FR-007); (b) empty selection (`schemas: ['db_pull_nonexistent']`) → truly header-only output (no banners, no CREATE SCHEMA/EXTENSION) + stderr warning via `console.warn` spy (FR-018); (c) baseline contains neither `GRANT ` nor `CREATE POLICY` nor `ROW LEVEL SECURITY` despite the seeded policy (FR-019) — fix any leaking renderer in `src/db-pull.ts`
- [ ] T016 [US2] Extension-filter tests in `src/__tests__/db-pull.test.ts`: conditional pg_trgm case (try `CREATE EXTENSION IF NOT EXISTS pg_trgm`, skip gracefully if unavailable, restore pre-existence state after): default output contains `CREATE EXTENSION IF NOT EXISTS pg_trgm` but NO DDL for any object whose name contains `gtrgm` in ANY section (functions/types/operators leak check); `includeExtensionObjects: true` → `gtrgm` function DDL present; verify parent-relation filtering (extension filter applied in constraints/ownership/triggers queries per plan) — fix leaks in `src/db-pull.ts` (SC-003, FR-008)

## Phase 5: US3 — CLI subcommand

- [ ] T017 [US3] Restructure `src/cli.ts`: move existing body into `runGenTypes(rawArgs)` unchanged, add dispatcher `run()` (`gen types` | `db pull` | print both usages + exit 1), add `parseDbPullArgs` (`--db-url`, `--schema/--schemas` comma-split accumulating, `--out`, `--mode`, `--include-extension-objects`, `--no-if-not-exists`, `--help`), `printDbPullUsage` (documents opt-out defaults + PG14+ trigger note), `utcTimestamp()` (`YYYYMMDDHHMMSS` from ISO string), `runDbPull` (DB_CONNECTION fallback; exact diff error `Only --mode baseline is supported in this version (diff is planned).` exit 1; stdout for `-`/`stdout`; default path `supabase/migrations/<ts>_baseline.sql` with `fs.mkdir recursive`; success log `Wrote baseline schema to <path>`)
- [ ] T018 [US3] Automated CLI tests `src/__tests__/db-pull-cli.test.ts` spawning `npx ts-node src/cli.ts db pull ...` via `child_process.execFile` with isolated env + temp cwd: `--help` exit 0 listing flags; missing db-url (env without DB_CONNECTION) → exit 1 + usage; `DB_CONNECTION` fallback works (env-injected, `--out -` emits header); `--mode diff` → exit 1 + exact message; default path run in temp dir creates `supabase/migrations/` + filename matches `/^\d{14}_baseline\.sql$/`; `--schema db_pull_schema,public` comma parsing (output contains `db_pull_schema` objects) (FR-001/002/017, SC-005)
- [ ] T019 [US3] Manual quickstart smoke per `specs/001-db-pull/quickstart.md` §2–§6 after `npm run build` (stdout, default path, mode guard, re-apply via psql if available, help) — record actual outputs in the commit message

## Phase 6: Polish & cross-cutting

- [ ] T020 [P] Update `README.md`: replace roadmap line "`supalite db pull` wrapper around `pg-schema-sync` for schema sync" with remaining future work (`--mode diff`), rewrite the "About `supabase db pull`" paragraph to point at the native command, add usage subsection under "Migrations and schema management" (flags, opt-out defaults, idempotency guarantee incl. constraint guards, extension filter, section order, v1 limitations incl. footer-listed variants, PG14+ trigger replay note)
- [ ] T021 [P] Mirror the same changes in `README.ko.md` (roadmap line, "`supabase db pull`에 대해서" paragraph, 마이그레이션 섹션 사용법)
- [ ] T022 [P] Add `CHANGELOG.md` entry under `## [Unreleased]` → `### Added` describing `supalite db pull` (issue #4): dependency-ordered baseline, idempotent defaults (constraint DO guards), extension-object exclusion, coverage list, v1 limitations
- [ ] T023 Full regression gate: `npm test && npm run lint && npm run build` all green; confirm `gen-types.test.ts` untouched and passing

## Dependencies

```
T001 → T002 → T003 → T004 → T005 → T006 → T007 → T008 → T009 → T010 → T011 → T012 → T013 → T014
                                                                                          ↓
                                                                              T015 → T016 (US2)
                                                                                          ↓
                                                                              T017 → T018 → T019 (US3)
                                                                                          ↓
                                                                  T020 ∥ T021 ∥ T022 → T023
```

- US1 (T005–T014) is strictly sequential: single module + single test file;
  each renderer slots into the fixed section order.
- US2 depends on all renderers (T014 done); US3 depends on final option
  semantics (T016 done); T020–T022 are parallelizable [P]; T023 is the gate.

## Implementation strategy

- **MVP = US1** (T001–T014): after T014 the library API delivers the full
  guarantee (round-trip + idempotency) without a CLI.
- Commit after every task (`feat(db-pull): ...` / `test(db-pull): ...` /
  `docs: ...`), keeping `npm test -- db-pull` green at each step.
- Every test command runs against live Postgres via `DB_CONNECTION`
  (default `postgresql://testuser:testpassword@localhost:5432/testdb`).
